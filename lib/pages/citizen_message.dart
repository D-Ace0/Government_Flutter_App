import 'package:flutter/material.dart';

class CitizenMessage extends StatelessWidget {
  const CitizenMessage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
